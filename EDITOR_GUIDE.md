# 设计规范编辑器

打开右上角“设计规范”即进入编辑模式。画布保持原始排版尺寸并缩放到剩余区域；窄屏使用上下分区。关闭后恢复预览。

## 编辑元素
- 点击文字编辑文案和文字样式；点击角色卡片空白处选择整张卡片。
- 蓝色选中框与面板名称对应。路径可直接选择祖先；↑选择父级，↓优先返回刚才的子级。
- 布局优先提供列数/方向、间距、对齐；卡片提供尺寸与外观；文字不显示容器属性。
- 宽高模式包括沿用页面、自动、适应内容、填满父容器、分配剩余空间、固定尺寸。填满父容器是100%；分配剩余空间遵循父布局的弹性主轴或拉伸对齐。
- 输入固定尺寸即时临时预览，取消、关闭面板或切换对象/页面会撤回未确认值。点击“存为规范并应用”才入库保存，同值复用已有尺寸规范。
- 内边距默认四边合并，不相同时显示混合值；可分别设置。外边距放在“更多布局”中。卡片间距在外层布局调整。
- 文字样式可组合字号、字重、行高、字色。已有单独字色优先于样式字色；旧规范未定义字色时保留原色。

## 规范库
- 当页列出当前页经编辑器绑定的规范；全局列出完整库。源码直接引用的全局变量在下方单独列出使用情况。
- 可按名称/用途搜索和分类。新增值需先入库再正式应用。
- 共享规范显示已绑定页数/元素数；修改前提示影响范围。修改后所有绑定同步生效。查看引用位置可高亮本页元素。
- 全局变量会影响所有使用它的页面；“本页 N 处”不是全局总数。

## 保存与撤销
- 文案、规范引用、旧覆盖、规范库、全局设置仍沿用原有 localStorage 键。新增库项补充合并，不清空旧数据。
- 撤销/重做覆盖上述编辑，当前会话最多100步；刷新后保留保存数据，历史记录不保留。
- 保存到浏览器不等于写回项目源码。底部摘要列出已保存改动，点击“复制改动发给 AI”后粘贴到对话交接。复制失败时可手动复制完整数据。
- 导入/导出变量用于全局变量，完整元素与库数据随AI交接载荷提供。

## 验证
独立浏览器测试在 `scripts/editor-checks/`。本次使用外部安装的 Playwright 与系统 Chrome，不依赖或更改用户浏览器配置：

```sh
PLAYWRIGHT_MODULE=/tmp/p7-editor-check/node_modules/playwright node scripts/editor-checks/library.cjs
PLAYWRIGHT_MODULE=/tmp/p7-editor-check/node_modules/playwright node scripts/editor-checks/compat.cjs
PLAYWRIGHT_MODULE=/tmp/p7-editor-check/node_modules/playwright node scripts/editor-checks/final.cjs
```

预览服务需在 localhost:3000 运行。测试里的 Chrome 路径可按本机调整。
