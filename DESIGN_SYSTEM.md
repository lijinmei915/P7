# P7 作品集设计规范

## 使用方式

网页右上角「设计规范」打开可视化编辑器。调整即时作用于页面并保存在当前浏览器；刷新仍保留。恢复默认撤回全部变量调整。导出 JSON 备份，导入可在另一浏览器复用。浏览器修改不会写回源代码；作为项目固定默认值时，同步修改 src/design/settings.ts 与 src/design/tokens.css。

## 变量与作用范围

| 变量 | 默认 | 用途 |
| --- | --- | --- |
| 品牌蓝 | #1853ff | 全部页面已迁移的品牌文字、图形和边框；插画自身的辅助蓝紫色保留 |
| 浅色画布 | #f4f6fb | 公共画布及引用该色的背景 |
| 深色画布 | #0a0d14 | 深色页面主背景 |
| 正文标题色 | #1e293b | 正文页标题及 slate-800 文本 |
| 深色卡片 | #172238 | 行业洞察系统要求卡片等强调表面 |
| 正文页标题 | 25px | deck-page 正文主标题；不影响章节艺术字或内部图表标题 |
| 页眉 | 14px | deck-page 页眉 |
| 说明文字字号 / 字重 | 13px / 400 | 洞察页中间说明与右侧说明共用的排版层级 |
| 正文页边距 | 40px | 标准正文页根容器；封面与履历独立处理 |
| 卡片圆角 | 16px | 画布内通用 xl/2xl/3xl 卡片；圆形标识和胶囊除外 |
| 画布圆角 | 48px | 公共 16:9 外框 |
| 流光周期 | 3.6s | 洞察页过渡流光；尊重系统减少动态效果设置 |

图表语义色、特定产品界面、章节插画和旧页面局部正文字号暂保留原值，避免全局替换破坏层级。参数极值不是排版验收保证，定稿前应逐页检查裁切与对比度。

## 固定规则

- 画布为 16:9。正文页沿统一页眉、标题与安全边距对齐。
- 文字层次：页标题 25px、模块标题 18px、正文 14px、注释 11px（后面三档为新页推荐值，尚未覆盖所有旧页面）。正文不得为了塞内容无止境缩小。
- 一页只承担一个主要结论；超量先拆页。标题允许自然换行时须为其预留空间。
- 深色用于封面、章节与关键结论，浅色用于分析和方案。蓝色为主强调色；图表同一指标保持一致编码。
- 卡片用来分组，不作为每页强制结构。每页最多一个持续动态焦点。

## 允许变化的构图

封面/章节：大标题与抽象艺术图形或产品片段。
背景/洞察：左右比较、趋势、因果推导。
研究/问题：证据、旅程与问题定位。
策略/架构：流程、角色关系与分层结构。
方案/细节：大幅真实界面与少量标注。
结果/复盘：前后对比、结果证据与取舍。

## 开发接入

原始变量 --ds-* → 语义变量 --color-primary / --slide-bg 等 → 组件变量 --slide-title-size / --slide-padding / --card-radius。新增正文页优先使用 StandardPageLayout，或根节点增加 deck-page 并遵守 header + 标题区域结构。艺术页可以例外，但主品牌色和画布必须复用变量。不要将蓝色警告/成功颜色统一替换为品牌色。

## 分级圆角

S 4px 用于标签和信息块；M 8px 用于流程节点、小卡片；L 16px 用于分组和图表容器；XL 48px 用于 PPT 画布。整体圆润度在 0–150% 之间按比例乘以各档基准值，各档可单独微调。胶囊、圆形保持全圆。

旧的 radius 和 canvas-radius 设置保留，分别作为 L 和 XL 基准值。现有完整圆角工具类按尺寸映射到 S/M/L，局部单侧圆角与特殊插画造型保留原样。
## 页面最小字号

页面所有文字不得低于 11px，包括正文、说明、编号、标签和辅助标注。辅助标注默认 11px；新增或修改文字规范必须满足此下限，旧元素字号覆盖也按至少 11px 显示。容器相对字号使用 max(11px, …) 保底。内容拥挤时调整空间和布局，不再缩小到 11px 以下。

## 尺寸与间距规范

容器宽度、高度可从规范库选择自动尺寸（auto）、适应内容（fit-content）、填满父容器（100%），或固定尺寸档：48、64、96、128、160、192、240、320、400、480、560、640px。单位为画布 CSS 像素，预览缩放不改变规范值；百分比以父容器为基准。固定尺寸可能超过父布局，需结合当前页面检查。

内边距、外边距、布局行距与列距统一从间距库选择：0、4、5、8、12、16、24、32、40、48px。卡片间距在外层 flex/grid 容器设置；文字元素仍只显示文字相关属性。新增固定尺寸（大于 0、最多 2000px）和间距须先入库，再引用；修改库中规范会同步所有引用。沿用页面样式可撤销单项覆盖。

## 编辑器交互（当前）

编辑器已采用“编辑元素 / 规范库”分区，使用与保存规则以 [EDITOR_GUIDE.md](EDITOR_GUIDE.md) 为准。尺寸支持临时预览与取消，确认后才入库；编辑器提供当前会话撤销重做。角色卡片等具体页面尚未全部迁移到排版变量，编辑器改进不等同于整套 PPT 已完成视觉规范化。
