# 设计编辑器改进记录

## 已实现并验证
- 编辑面板与画布分区，保留画布原始宽度并缩放；1741×1100 下画布右边1325，面板左边1345。
- 编辑元素 / 规范库分离；元素层级路径、父子箭头、独立选中框。
- 根据文字、布局、卡片筛选属性。
- 尺寸模式、固定尺寸取消预览、确认入库；同值复用已有规范。
- 合并四边内边距、分别设置、折叠外边距。
- 文案、样式、库与全局设置共用撤销记录；当前会话最多100次，刷新后保留数据，不保留历史。
- 共享规范显示已绑定页面/元素数量，保存前显示影响提示；可高亮本页引用。
- 规范库名称/用途搜索与分类。
- 页眉和底部固定，属性滚动；改动摘要与复制反馈。

## 本轮运行证据
`/tmp/p7-editor-check/check.cjs`：尺寸预览435.939→349.984→取消435.939；应用/撤销/重做/刷新正确；文案修改/撤销/重做正确；四边内距16px、撤销0px。
`/tmp/p7-editor-check/library.cjs`：带断言，两个绑定元素的共享尺寸更新、撤销、影响范围、高亮、剪贴板、摘要、固定操作栏全部通过。
脚本副本在 scripts/editor-checks，可设置 PLAYWRIGHT_MODULE 指向独立安装的 playwright。测试使用独立临时浏览器，不读写用户正在使用的浏览器设置。

## 最终验收

| 目标 | 当前实现与验证证据 |
| --- | --- |
| 画布与面板不遮挡 | compat.cjs 对1741×1100、1280×800、390×844逐项断言无重叠；关闭清除zoom。 |
| 清晰选择与层级 | SelectionOutline独立浮层；final.cjs验证父级/子级返回H3；元素标题在滚动区sticky。 |
| 上下文属性 | final.cjs验证布局有列间距无背景色，文字无宽度模式。 |
| 宽高与间距 | DimensionControl模式、剩余空间主轴；final.cjs验证flexGrow=1及列间距24px；interactions记录合并padding16→撤销0。 |
| 可取消预览 | interactions记录预览/取消/确认；compat验证关闭取消；final验证切页取消。 |
| 撤销重做 | interactions验证尺寸/文案/间距，library验证共享规则，compat验证全局变量。 |
| 编辑/规范库分离 | DesignPanel mode分区；final验证当页隐藏未绑定640档、全局可见。 |
| 共享影响范围 | library断言2元素/1页影响提示、引用高亮、同步更新与撤销。 |
| 入库与交接反馈 | library断言相同350尺寸复用、摘要、剪贴板内容与固定顶部/底部。 |
| 保留旧数据 | compat预置旧库、旧样式覆盖和全局自定义色，验证保留及撤销。 |
| 文字组合规范 | final断言14px/700/标题色组合，并保留独立品牌色覆盖。 |
| 文档和构建 | EDITOR_GUIDE.md已更新；tsc --noEmit和Vite build通过，仅原有chunk体积提示。 |

测试运行在独立临时浏览器上下文，未改动用户的浏览器数据。测试截图在 /tmp/p7-editor-check/，脚本在 scripts/editor-checks/。

本次目标为编辑器体验，未宣称所有旧PPT页面都已经迁移到统一视觉变量。元素数据仍使用既有DOM路径；后续大幅更改PPT结构时需核对已有引用。撤销记录限当前会话；正式同步源码仍通过复制交接数据给AI完成。
